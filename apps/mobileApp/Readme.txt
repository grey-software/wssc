This App will run by following the steps below:

.First run the command 'npm install'(to install all the neccessary Modules) inside the wsscMobileApp folder
.Then, run the command 'npx expo start', The App will run.
.Along with this, the server should also be running.